- name: "Create server directory"
  file:
    path: "~/server"
    state: "directory"
    mode: 0755

- name: "Compress and copy backend file"
  copy:
    src: "artifact.tar.gz"
    dest: "~/server/artifact.tar.gz"

- name: "Extract backend to folder"
  unarchive:
    remote_src: yes
    src: "~/server/artifact.tar.gz"
    dest: "~/server"

- name: "Start web server"
  shell:
    cmd: |
      cd ~/server
      npm install
      pm2 stop default
      pm2 start npm -- start
